diff --git a/dist/adapters/node/request.mjs b/dist/adapters/node/request.mjs
index 2d0fa6aabe44f34fbd46ae56beddcc5ada4fd955..d1cede4db39474692bb697304508f974c1915d02 100644
--- a/dist/adapters/node/request.mjs
+++ b/dist/adapters/node/request.mjs
@@ -160,13 +160,13 @@ async function setResponse(res, response) {
 			for (;;) {
 				const { done, value } = await reader.read();
 				if (done) break;
-				if (!res.write(value)) if (process.env.AWS_LAMBDA_FUNCTION_NAME || process.env.LAMBDA_TASK_ROOT) continue;
-				else {
+				if (!res.write(value)) {
+					if (process.env.AWS_LAMBDA_FUNCTION_NAME || process.env.LAMBDA_TASK_ROOT) continue;
 					res.once("drain", next);
 					return;
 				}
-				res.end();
 			}
+			res.end();
 		} catch (error) {
 			cancel(error instanceof Error ? error : new Error(String(error)));
 		}
