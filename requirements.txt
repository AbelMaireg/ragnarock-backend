# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Scalar docs
scalar-fastapi==1.0.3

# Validation
# pydantic-core wheels are not yet published for Python 3.14+;
# using >= lets pip pick the latest compatible pre-release/wheel.
pydantic>=2.10,<3
pydantic-settings>=2.7,<3

# LLM providers
google-generativeai==0.8.3
# Vertex AI / Gemini Enterprise (ADC) — also supports api_key mode via Client(api_key=...)
google-genai>=1.74,<2
anthropic==0.40.0
openai==1.58.1

# File parsing
pypdf==5.1.0
python-docx==1.1.2

# URL extraction
httpx==0.28.1
beautifulsoup4==4.12.3

# Vector memory (Phase 3)
asyncpg==0.30.0
pgvector==0.3.6
sqlalchemy[asyncio]==2.0.36

# Queue + upload storage
redis>=5.2,<6
boto3>=1.35,<2

# Multi-agent orchestration (Phase 4)
langgraph>=0.2,<0.4

# Utilities
python-multipart==0.0.20
python-dotenv==1.0.1
tenacity==9.0.0
